import { StyleSheet } from "react-native";

const PlayerViewStyle = StyleSheet.create({
  PlayerCard: {
    width: "100%",
    borderRadius: 5,
    borderBottomWidth: 1,
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
  },
});

export default PlayerViewStyle;
